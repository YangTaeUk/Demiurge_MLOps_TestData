name: foodcom_to_mongodb
mode: batch
generator:
  type: foodcom
  batch_size: 5000
handler:
  format: json
  compression: none
adapter:
  type: mongodb
  host: localhost
  port: 27017
  extra:
    username: testdata
    password: testdata_dev
    database: testdata
