name: store_sales_to_postgresql
mode: batch
generator:
  type: store_sales
  batch_size: 10000
handler:
  format: json
  compression: none
adapter:
  type: postgresql
  host: localhost
  port: 5434
  extra:
    user: testdata
    password: testdata_dev
    database: testdata
